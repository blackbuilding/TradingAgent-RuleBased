# MT5 account credentials, read by the rule-strategies agent at session
# start via the mcp-metatrader5-server `login()` tool. Copy this file to
# `.env` and fill in real values. `.env` is gitignored — never commit real
# credentials.
MT5_LOGIN=
MT5_PASSWORD=
MT5_SERVER=
